# 可行性评估与一周执行计划(Phase 1~4)

> 本文档回答一个问题:**「能开源就开源,加上 AI 辅助,一周内能否在开发板上跑通?」**
> 结论基于 2026-08-17 对代码库现状的审查,以及已确认的三个前提条件。
> 权威技术细节以 `IMPLEMENTATION_PLAN.md` 为准,本文档是它的**执行视角补充**。
> ⚠️ 运行方式已于 **2026-08-26 更新为宿主机本地直接运行(无 Docker 容器)**,涉及环境 / 排期 / 风险的条目已同步调整;所有下载/编译/安装遵循就绪检查惯例(`docs/00` §4)。

---

## 0. 结论摘要(Executive Summary)

| 问题 | 结论 |
|---|---|
| 全 6 个 Phase(含语言嵌入 3DGS + 文本查询)一周跑通? | **不现实**,正常节奏 4~8 周 |
| **Phase 1~4**(数据流 + RGB-D 3DGS-SLAM 基线 + 基础优化 + ROS2 接口)一周跑通? | **现实可行**,但有 1 个关键风险点 |
| 项目 90% 组件是否开源? | 是,SplaTAM / diff-gaussian-rasterization / librealsense / ROS2 / MobileSAM / MobileCLIP 全部开源 |
| 项目代码现状 | **文档 + 骨架**,算法代码为 0 行,从零开始 |

一句话:**一周可以跑通一个有意义的 SLAM 基线,但跑不通整个语言嵌入系统,且成败取决于一个 CUDA 扩展能否在板子上编译。**

---

## 1. 项目现状(审查结论)

| 目录 | 现状 | 说明 |
|---|---|---|
| `docs/` + `IMPLEMENTATION_PLAN.md` | ✅ 完整 | 技术路线质量很高,公式/接口/验收标准齐全 |
| `ws_src/`(ROS2 消息) | ✅ 骨架 | 5 个 `.msg` 已定义 |
| `docker/` | ✅ 备用 | Dockerfile / compose / entrypoint（**主路径已改为宿主机本地运行**,见 `docs/00` §1） |
| `config/` | ✅ 骨架 | 相机/SLAM/特征/ROS2 四个 YAML |
| `src/edge_3dgs_slam/` | ⚠️ **空** | 只有空 `__init__.py`,算法代码 0 行 |
| `experiments/` | ⚠️ 空 | 无任何可运行脚本 |
| `third_party/` | ✅ 已就绪 | SplaTAM 已 clone；**Replica 数据集已下载**至 `third_party/SplaTAM/data/Replica/`（8 场景×2000 帧，见 docs/02 §7） |
| `data/` | ✅ 已就绪 | `outputs/`（Phase 1-3/5 产出）+ `checkpoints/`（MobileSAM/CLIP 权重）+ `raw/`（测试图） |

**核心判断**:这是一个设计完整的项目骨架,**没有任何可运行的代码**。一周内要做的是"从零实现",而非"调通已有工程"。

---

## 2. 目标范围与前提(已确认)

| 项 | 确认值 |
|---|---|
| **目标范围** | **Phase 1~4**(非全 6 Phase) |
| **开发板** | Jetson Orin NX 16GB(已烧录 JetPack 6.2,可开机联网) |
| **相机** | Intel RealSense D435i(已到手) |
| **开发方式** | **全程在板子上开发**(本机即目标机,无跨架构搬运) |

**Phase 1~4 的定义**:
- **Phase 1**:D435i 数据流打通(宿主机本地:pyrealsense2 / ROS2 → 对齐 RGB-D + IMU + 内参)
- **Phase 2**:RGB-D 3DGS-SLAM 基线(Tracking + Mapping,免 COLMAP)
- **Phase 3**:基础优化(关键帧管理 / 高斯剪枝 / 视锥剔除)— 一周内做**基础版**
- **Phase 4**:ROS2 具身接口(订阅 RGB-D,发布 `/tf` + 高斯点云)

**明确排除在一周范围外**:Phase 5(MobileSAM+MobileCLIP 特征工厂)与 Phase 6(语言嵌入 3DGS + 文本查询),留作后续阶段。

---

## 3. 可行性判断:为什么一周"可行"也"有风险"

### 3.1 有利因素(把一周从不可能变可能)

1. **板子与相机已就绪**——排除了最大的时间黑洞(环境搭建/硬件调试)。
2. **全程板端开发**——编译、运行、调试同机,无跨架构往返。
3. **90% 组件开源且成熟**——librealsense、ROS2、graphdeco rasterizer 都是活跃维护的成熟项目,我们做的是**移植 + 集成 + 优化**,不是发明。
4. **`IMPLEMENTATION_PLAN.md` 提供了全部核心公式与接口契约**——Tracking/Mapping 的损失函数、参数化、关键帧逻辑都写清楚了,可在 AI 辅助下直接翻译成代码。
5. **AI 辅助**——大量机械性、模板性代码(数据结构、IO、脚本、ROS2 节点骨架)可以大幅提速。

### 3.2 关键风险点(整个计划就这一个"成败开关")

> **SplaTAM 的 diff-gaussian-rasterization CUDA 扩展能否在 aarch64 / sm_87 上编译通过。**

- 计划附录 B 将它标为**高风险**,这是全项目最大的不确定项。
- 影响:编译通过 → 后续很顺,一周达成 Phase 1~4;编译受阻 → 必须先降级(纯 PyTorch 慢速 splat 保正确性),"实时"目标延后。
- 对策见下一节"策略调整"。

### 3.3 一周的期望管理

- **"跑通" ≠ "实时"**。计划中的 **15–30 FPS 是 Phase 3 的长期优化目标,不是一周内能保证的**。SplaTAM 在桌面 GPU 上约 ~10 FPS,Orin NX 上一周内能做到"跑起来、结果正确",实时性留到后续优化。
- 一周的合理成果:**D435i 输入 → SLAM 跑通 → 输出轨迹 + 高斯地图 → ROS2 发布**。这已是可演示、可写简历的里程碑。

---

## 4. 关键策略调整:不要原样跑 SplaTAM 仓库

计划原稿思路是"SplaTAM 蓝本,clone 下来改"。**建议调整**,理由:

- SplaTAM 是研究仓库(非 pip 包),即使在桌面 x86 也以难跑著称:锁旧 torch 1.12、带 GUI 依赖、rasterizer 是 2023 年的老 fork。
- 在 aarch64 + torch 2.x(Jetson PyTorch for Jetson)下,预期需要打多处补丁,**一周的紧排期经不起这个不可控**。

### 4.1 三条腿并行铺开

**① 主力腿(最稳)——官方 rasterizer + 自写 SLAM 循环**

| 组件 | 来源 | 说明 |
|---|---|---|
| `diff-gaussian-rasterization` | `graphdeco-inria` 官方仓库(活跃维护) | torch 2.x 兼容;编译只须 `TORCH_CUDA_ARCH_LIST="8.7"` |
| depth 渲染通道 | 参考 SplaTAM 的扩展实现自行补充 | 官方 rasterizer 不渲染 depth,需加一个输出通道(有现成实现可抄) |
| Tracking / Mapping 循环 | **按 `IMPLEMENTATION_PLAN.md` 2.4 节公式自写** | 约 500~800 行;公式、损失、关键帧逻辑文档已齐 |

**② 降级腿——纯 PyTorch 慢速 splat**

- 若 CUDA 扩展编译受阻,先用**纯 PyTorch 自动求导**实现慢速 splat(显式深度排序 + α 混合)。
- 目的:**先验证数学正确性**,再并行攻 CUDA 编译。
- 这是计划 2.5.3 已写明的退路。

**③ 并行腿——Phase 5 铺路(可选,空闲时做)**

- MobileSAM + MobileCLIP 与 SLAM **互不依赖**(计划 0.4),可并行验证单帧掩码 + 特征。
- 只作 week 2 语言嵌入的铺垫,**不占 Phase 1~4 的排期**。

> **决策点**:开工 D1 先快速试 SplaTAM fork rasterizer 的编译(它有现成 depth 通道);若 1 天内反复受阻,立即切到主力腿(官方 rasterizer + 自写)。不恋战。

---

## 5. 一周排期(板端,Phase 1~4)

| 天 | 内容 | 产出 / 验收 |
|---|---|---|
| **D1** | 环境盘点 + 本地环境收尾(就绪检查) | 宿主机 `torch.cuda.get_device_capability() == (8,7)`;补齐 pyrealsense2(见 docs/01 §2) |
| **D2** | pyrealsense2(免编译)+ D435i 数据流 + 深度反投影验证 | Phase 1 验收通过:RGB-D 对齐、`K` 合理、反投影出 `.ply` 无错位 |
| **D3–D5** | **主力攻坚**:rasterizer 编译(先就绪检查,见 docs/00 §4)+ depth 通道 + Tracking/Mapping 写通 + Replica 数据集验证 | Phase 2 验收:ATE 达 cm 级,输出渲染图 + `.ply`,数值/解析梯度互验 |
| **D6** | D435i 自采序列跑通 + 基础优化(关键帧 / 剪枝 / 视锥剔除) | 长序列稳定不崩,无 OOM,给出基础 FPS 数据 |
| **D7** | ROS2 节点封装 + 收尾 | Phase 4 最小可用:`/tf` + 高斯点云发布;`colcon build` 通过;留 1 天 buffer |

**关键路径**:D3–D5 的 rasterizer 编译 + SLAM 循环实现,是全周的关键路径,需在 D3 就立即启动。

**验证数据口径**（docs/00 §7 统一约定）：机制验证先用合成序列（Phase 2 已建，秒级可复现）；
算法性能/文献对比用 **Replica**（已下载，ATE 可与 SplaTAM 论文对照）；真机/实时验证用
**D435i 自采**（Phase 1 数据流）。绝对指标标注 数据来源 + 分辨率 + 帧数。

---

## 6. 各 Phase 验收标准对照(一周目标版)

| Phase | 完整验收标准(计划原文) | 一周内必须达成 | 可延后 |
|---|---|---|---|
| 1 | CUDA (8,7)、话题数据流、反投影 `.ply` 无错位 | ✅ 全达成 | — |
| 2 | Replica ATE cm 级、渲染图 + `.ply`、梯度校验、`build_map` 接口 | ✅ 全达成 | — |
| 3 | **15–30 FPS**、显存预算表、量化消融表 | 基础版:关键帧/剪枝/视锥;FPS 数据如实记录 | 实时目标、完整消融表 |
| 4 | `/tf` + `/gaussian_map` 稳定发布、rviz2 可视化、`colcon build` 通过 | ✅ 最小可用 | 完整可视化、`Query` 服务(P6) |

---

## 7. 风险清单与缓解(一周视角)

| # | 风险 | 等级 | 缓解 |
|---|---|---|---|
| R1 | rasterizer 在 aarch64/sm_87 编译失败 | **高** | 官方 rasterizer + `TORCH_CUDA_ARCH_LIST="8.7"` + 宿主 GCC;**1 天为限**,受阻即切纯 PyTorch 降级腿 |
| R2 | 系统级依赖与 JetPack 冲突(如 numpy 被升到 2.x、torch 版本错) | 中 | 锁 `numpy<2`、torch 用 PyPI aarch64 wheel;按就绪检查惯例(docs/00 §4)逐项核对,不整包升级 |
| R3 | 国内网络拉取 pip / apt / github 依赖失败 | 中 | 提前确认代理;pip 可配国内镜像源;依赖总量远小于 docker 镜像 |
| R4 | D435i 驱动 / 时间戳异常 | 中 | RSUSB 用户态后端(免内核 patch)+ udev 规则 + 硬件时间戳 + ApproximateTimeSynchronizer |
| R5 | 16GB 统一内存 OOM(SAM/CLIP 与高斯训练并存) | 中 | 一周内 Phase 1~4 不含 SAM/CLIP,天然规避;仅需高斯侧剪枝管理 |
| R6 | Tracking 实时性不达标(期望管理) | 中 | 一周内以"跑通 + 结果正确"为目标,实时性明确列为后续优化 |

---

## 8. 环境盘点清单(开工 D1 第一件事)

以下各项必须逐项确认,是 Phase 1 的入场券(**宿主机本地运行,无容器**;按就绪检查惯例逐项核对,已就绪的跳过):

1. **架构确认**
   - `uname -m` 应为 `aarch64`
   - `nvidia-smi` 能识别 Ampere GPU(Orin NX,1024 CUDA 核)
2. **JetPack 版本**
   - `cat /etc/nv_tegra_release` 或 `nvcc --version`,确认 **JetPack 6.2 / L4T 36.4 / CUDA 12.x**
3. **软件栈就绪检查(2026-08-26 已核实大部分就绪)**
   - `python3 -c "import torch; print(torch.__version__, torch.version.cuda)"` → **已就绪**:2.11.0 / CUDA 12.6 ✅
   - `ls /opt/ros/humble/setup.bash` → **已就绪**:ROS2 Humble ✅
   - `python3 -c "import numpy; print(numpy.__version__)"` → **已就绪**:1.26.4(<2)✅
   - `python3 -c "import pyrealsense2"` → **未就绪**,Phase 1 D2 补齐(docs/01 §2)
   - 其余未就绪项按各 Phase 文档 + `docs/00` §4 惯例补齐
4. **磁盘空间**
   - 预留 **~15GB**:torch wheel + Python 依赖 + Replica 数据集(无需 docker 镜像)

---

## 9. 下一步行动

1. **确认环境盘点清单**(第 8 节),把结果回填到本文档。
2. 按 D1 开始:**宿主机就绪检查(`docs/00` §4 惯例)→ 补齐 pyrealsense2 → 验证 CUDA (8,7)**。
3. 同步验证 D2 前置:USB 权限、udev 规则(`docs/01` §3)。
